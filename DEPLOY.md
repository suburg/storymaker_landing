# 🚀 Инструкция по деплою Landing Page "СочиНяшка"

Подробное руководство по развёртыванию на различных платформах.

---

## 📋 Содержание

1. [Vercel (рекомендуется)](#vercel-рекомендуется)
2. [Netlify](#netlify)
3. [Cloudflare Pages](#cloudflare-pages)
4. [GitHub Pages](#github-pages)
5. [Docker на VPS](#docker-на-vps)
6. [Сравнение платформ](#сравнение-платформ)

---

## Vercel (рекомендуется) ⭐

**Почему Vercel:**
- ✅ Оптимизирован для Nuxt.js
- ✅ Бесплатный SSL (автоматически)
- ✅ Глобальный CDN
- ✅ Автодеплой из Git
- ✅ Preview deployments для PR

### Шаг 1: Подготовка

```bash
# Убедитесь, что проект в Git
git add .
git commit -m "feat: готово к деплою"
git push origin main
```

### Шаг 2: Деплой

**Вариант A: Через веб-интерфейс**

1. Перейдите на https://vercel.com
2. Нажмите "Add New" → "Project"
3. Импортируйте ваш Git репозиторий
4. Vercel автоматически определит Nuxt 3
5. Настройки по умолчанию подходят
6. Нажмите "Deploy"

**Вариант B: Через CLI**

```bash
# Установить Vercel CLI
npm install -g vercel

# Войти
vercel login

# Деплой
vercel

# Production деплой
vercel --prod
```

### Шаг 3: Подключение домена

1. В панели Vercel → Settings → Domains
2. Добавьте `socinyashka.ru`
3. Настройте DNS записи (Vercel покажет инструкции)
4. SSL сертификат выдастся автоматически (1-5 минут)

### Шаг 4: Environment Variables (если нужно)

Settings → Environment Variables → Добавить переменные

**Готово!** 🎉  
Ваш сайт доступен на `https://ваш-проект.vercel.app`

---

## Netlify

**Преимущества:**
- ✅ Простой в использовании
- ✅ Бесплатный SSL
- ✅ Формы без бэкенда
- ✅ Functions (serverless)

### Шаг 1: Подготовка

Создайте файл `netlify.toml`:

```toml
[build]
  command = "npm run generate"
  publish = ".output/public"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
```

### Шаг 2: Деплой

**Через веб-интерфейс:**

1. Перейдите на https://netlify.com
2. "Add new site" → "Import an existing project"
3. Выберите ваш Git репозиторий
4. Build settings определятся автоматически
5. "Deploy site"

**Через CLI:**

```bash
# Установить Netlify CLI
npm install -g netlify-cli

# Войти
netlify login

# Деплой
netlify deploy

# Production деплой
netlify deploy --prod
```

### Шаг 3: Custom Domain

1. Site settings → Domain management
2. Add custom domain → `socinyashka.ru`
3. Настроить DNS (A-запись на Netlify IP)
4. SSL автоматически

**Готово!** 🎉

---

## Cloudflare Pages

**Преимущества:**
- ✅ Самый быстрый CDN
- ✅ Бесплатно навсегда
- ✅ Unlimited bandwidth
- ✅ Встроенная защита от DDoS

### Шаг 1: Деплой

1. Перейдите на https://pages.cloudflare.com
2. "Create a project" → Подключите Git
3. Build settings:
   - Build command: `npm run generate`
   - Build output: `.output/public`
4. Deploy

### Шаг 2: Custom Domain

1. Pages → Custom domains
2. Добавить `socinyashka.ru`
3. Если домен уже в Cloudflare - автоматически
4. Если нет - настроить NS записи

**Готово!** 🎉

---

## GitHub Pages

**Преимущества:**
- ✅ Бесплатно
- ✅ Простая интеграция с GitHub
- ✅ Автоматический SSL для custom domains

### Шаг 1: Настройка

Создайте `.github/workflows/deploy.yml`:

```yaml
name: Deploy to GitHub Pages

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      
      - name: Setup Node
        uses: actions/setup-node@v3
        with:
          node-version: '20'
      
      - name: Install dependencies
        run: npm ci
      
      - name: Generate static site
        run: npm run generate
      
      - name: Deploy to GitHub Pages
        uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: .output/public
```

### Шаг 2: Настройка репозитория

1. Settings → Pages
2. Source: Deploy from a branch
3. Branch: `gh-pages` (создастся автоматически)
4. Save

### Шаг 3: Custom Domain

1. Settings → Pages → Custom domain
2. Добавить `socinyashka.ru`
3. Настроить DNS:
   - A-запись на GitHub IPs
   - CNAME на `username.github.io`
4. Включить "Enforce HTTPS"

**Готово!** 🎉

---

## Docker на VPS

**Когда использовать:**
- У вас уже есть VPS
- Нужен полный контроль
- Планируете размещать несколько сервисов

### Требования

- VPS с Docker (DigitalOcean, Hetzner, AWS EC2)
- Ubuntu 20.04+ или Debian 11+
- Минимум 1GB RAM, 1 CPU

### Шаг 1: Подготовка сервера

```bash
# Подключиться к серверу
ssh root@ваш-сервер-ip

# Обновить систему
apt update && apt upgrade -y

# Установить Docker
curl -fsSL https://get.docker.com -o get-docker.sh
sh get-docker.sh

# Установить Docker Compose
apt install docker-compose -y

# Установить Make
apt install make -y
```

### Шаг 2: Клонировать репозиторий

```bash
# Клонировать проект
cd /opt
git clone https://github.com/ваш-username/storymaker_landing.git
cd storymaker_landing
```

### Шаг 3: Деплой

**Вариант A: Через Makefile (рекомендуется)**

```bash
# Первый деплой
make docker-deploy

# Проверить статус
docker ps

# Посмотреть логи
make docker-logs

# Обновление в будущем (одна команда!)
make docker-update
```

**Вариант B: Через Docker Compose**

```bash
# Запустить
docker-compose up -d

# Проверить
docker-compose ps

# Логи
docker-compose logs -f

# Остановить
docker-compose down
```

### Шаг 4: Настройка домена

**A-запись в DNS:**
```
socinyashka.ru → IP_вашего_сервера
```

### Шаг 5: SSL (Let's Encrypt)

См. подробную инструкцию в [`docs/SSL-SETUP.md`](docs/SSL-SETUP.md)

**Быстрый вариант с nginx-proxy:**

```bash
# Создать сеть
docker network create web

# Запустить nginx-proxy + certbot
# (см. docker-compose.prod.yml в SSL-SETUP.md)
```

### Шаг 6: Автоматические обновления

**Настроить webhook или cron:**

```bash
# Добавить в crontab
crontab -e

# Проверять обновления каждые 30 минут
*/30 * * * * cd /opt/storymaker_landing && make docker-update >> /var/log/deploy.log 2>&1
```

**Или через GitHub Webhook:**
- Settings → Webhooks → Add webhook
- Payload URL: `http://ваш-сервер/webhook`
- Настроить простой webhook handler

**Готово!** 🎉

---

## Сравнение платформ

| Параметр | Vercel | Netlify | Cloudflare | GitHub Pages | VPS + Docker |
|----------|--------|---------|------------|--------------|--------------|
| **Сложность** | ⭐ Легко | ⭐ Легко | ⭐ Легко | ⭐⭐ Средне | ⭐⭐⭐ Сложно |
| **Стоимость** | 💰 Бесплатно | 💰 Бесплатно | 💰 Бесплатно | 💰 Бесплатно | 💰💰 $5-10/мес |
| **SSL** | ✅ Авто | ✅ Авто | ✅ Авто | ✅ Авто | ⚠️ Настройка |
| **CDN** | ✅ Да | ✅ Да | ✅ Лучший | ⚠️ Ограничен | ❌ Нет |
| **Auto Deploy** | ✅ Да | ✅ Да | ✅ Да | ✅ Да | ⚠️ Настройка |
| **Custom Domain** | ✅ Да | ✅ Да | ✅ Да | ✅ Да | ✅ Да |
| **Bandwidth** | ⚠️ 100GB/мес | ⚠️ 100GB/мес | ✅ Unlimited | ⚠️ 100GB/мес | ✅ Зависит от VPS |
| **Контроль** | ⚠️ Ограничен | ⚠️ Ограничен | ⚠️ Ограничен | ⚠️ Ограничен | ✅ Полный |

---

## 🎯 Рекомендация для вашего проекта

### Для Landing Page "СочиНяшка":

**🥇 Первое место: Vercel**
- Идеально для Nuxt.js
- Нулевая настройка
- Лучший developer experience

**🥈 Второе место: Netlify**
- Отличная альтернатива
- Проще интерфейс
- Больше дополнительных функций (формы, functions)

**🥉 Третье место: Cloudflare Pages**
- Самый быстрый CDN
- Unlimited bandwidth
- Отличная защита

**Docker на VPS** - только если:
- У вас уже есть VPS
- Нужны другие сервисы на том же сервере
- Требуется полный контроль

---

## 📝 Чек-лист после деплоя

- [ ] Сайт открывается по основному URL
- [ ] HTTPS работает (зелёный замок)
- [ ] Все изображения загружаются
- [ ] Favicon отображается
- [ ] Open Graph работает (проверить в Facebook Debugger)
- [ ] Темы переключаются корректно
- [ ] Все ссылки работают
- [ ] Форма/модальные окна работают
- [ ] Проверено на мобильных устройствах
- [ ] Lighthouse score > 90

---

## 🔧 Troubleshooting

### "Build failed"

**Проблема:** Ошибка при сборке

**Решение:**
```bash
# Проверить локально
npm run generate

# Проверить зависимости
rm -rf node_modules package-lock.json
npm install
npm run generate
```

### "SSL certificate error"

**Проблема:** HTTPS не работает

**Решение:**
- Подождите 5-10 минут (SSL выдаётся автоматически)
- Проверьте DNS настройки
- Убедитесь, что домен корректно указывает на сервер

### "404 on refresh"

**Проблема:** При обновлении страницы появляется 404

**Решение:**
- Убедитесь, что настроен SPA fallback
- Для Vercel/Netlify это автоматически
- Для Docker проверьте nginx.conf (`try_files $uri $uri/ /index.html`)

---

## 📚 Дополнительные ресурсы

- [Vercel Documentation](https://vercel.com/docs)
- [Netlify Documentation](https://docs.netlify.com/)
- [Cloudflare Pages Documentation](https://developers.cloudflare.com/pages/)
- [Nuxt Deployment](https://nuxt.com/docs/getting-started/deployment)
- [Docker Documentation](https://docs.docker.com/)

---

**Успешного деплоя!** 🚀
